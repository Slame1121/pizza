{{ header }}
<section class="not_found page">
    <div class="container">
        <div class="page_content">
            <h1>{{ heading_title }}</h1>
            <p>{{ text_error }}</p>
        </div>

        {{ content_bottom }}
    </div>

</section>

{{ footer }}
